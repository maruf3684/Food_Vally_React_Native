import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      padding: 20,
    },

    gg:{
      color:"red",
      fontSize:29,
    }
  });
export default styles